import React from 'react';
import { Grid,Carousel,NoticeBar } from 'antd-mobile';
import PropTypes from 'prop-types'
import Title from '../common/Title'

const Task=(props)=>{
  
  return (
  <div className='user'>
    <Title content='工作'></Title>
    
  </div>
)}

Task.propTypes = {
  
}

export default Task